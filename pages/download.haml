:ruby
  # :alias => :download

:textile
  h1. Download and Installation
  
  h2. Rails 3.0
  
  Your prayers were heard - Apotomo runs on Rails 3.0! The *1.x mainline* is dedicated to Rails 3.0.
  
  Gems rule.

:coderay
  #!bash
  $ gem install apotomo --pre

:textile  
  h2. Rails 2.3

  The 0.1.x line runs with Rails 2.3.
  
:coderay
  #!bash
  $ gem install apotomo

:textile
  In your @environment.rb@ should be something like

:coderay
  #!ruby
  config.gem "apotomo"
  
:textile
  And, if you like jQuery better than prototype:

:coderay
  #!ruby
  Apotomo.js_framework = :jquery

:textile
  Be sure to check out the #{page_link "introduction guide", 'introduction'}.
